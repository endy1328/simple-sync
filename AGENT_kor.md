# 에이전트 운영 가이드

이 문서는 `simple sync` 프로젝트에서 작업하는 에이전트를 위한 한국어 문서입니다.
`AGENT.md`가 변경되면 같은 변경에서 이 파일도 함께 업데이트합니다.

## 프로젝트 목표

`simple sync`라는 간단한 Windows 애플리케이션을 만들고 유지합니다. 이 앱은 설정된 소스 디렉터리에서 타겟 디렉터리로 파일을 동기화합니다.

## 작업 규칙

- 앱은 단순하고 로컬 중심으로 유지합니다.
- 소스에서 타겟으로 가는 단방향 복사 동작을 유지합니다.
- pair 모드 기본값은 `copy`이며, 새 파일 또는 변경 파일만 복사하고 타겟에만 있는 파일은 유지합니다.
- `mirror` 모드는 명시적인 pair별 옵션으로만 허용하며, 복사 단계가 성공한 뒤 타겟에만 있는 파일을 삭제합니다.
- 명시 요청이 없으면 양방향 동기화를 추가하지 않습니다.
- 대용량 파일은 임시 파일에 먼저 복사하고 성공한 뒤 타겟으로 교체하는 안전한 방식을 유지합니다.
- 런타임 동기화 진행률은 저장 설정인 `SyncPair`와 분리합니다.
- 설정은 실행 파일 옆의 `config.toml`에 유지합니다.
- 개발 중 설치본과 `dotnet run` 실행본을 나란히 비교할 수 있도록 Debug 빌드는 Release와 다른 단일 인스턴스 Mutex를 사용합니다.
- 배포 버전은 `VERSION`에 기록하고, `CHANGELOG.md`와 프로젝트/설치 버전을 함께 맞춥니다.
- 구현 변경 후 README와 하네스 문서를 최신 상태로 유지합니다.
- 현재 작업과 무관한 사용자 변경을 덮어쓰지 않습니다.
- 변경 범위를 작게 유지하고, 인계 전 `dotnet build`와 콘솔 동기화 테스트로 검증합니다.

## 안전 규칙

- 타겟 경로가 소스와 같거나 소스 내부에 있으면 거부하거나 건너뜁니다.
- 없는 소스, 잠긴 파일, 권한 오류는 보고 가능한 동기화 실패로 처리합니다.
- 복구 가능한 파일 시스템 오류가 발생해도 다른 파일과 다른 쌍 처리는 계속합니다.
- 일시적인 파일 복사 실패는 짧게 재시도한 뒤 최종 실패로 기록합니다.
- 파괴적인 파일 작업은 명시적인 `mirror` 모드로만 제한합니다.
- 해당 pair에서 소스 탐색 또는 복사 실패가 있으면 mirror 삭제 단계는 건너뜁니다.

## 문서 규칙

운영 문서와 하네스 문서는 영어와 한국어 쌍으로 유지합니다.

- `AGENT.md`와 `AGENT_kor.md`
- `HARNESS.md`와 `HARNESS_kor.md`

운영 동작, 검증 방식, 프로젝트 워크플로가 바뀌면 두 파일을 함께 업데이트합니다.
