# simple sync 1.2.1

## 변경 사항

- 언어 변경 시 화면 요소가 순차적으로 천천히 바뀌어 보이던 현상을 줄이도록 UI redraw를 일괄 처리하게 개선했습니다.
- 언어 적용 중 Activity 필터 변경 이벤트가 중간 렌더를 일으키지 않도록 조정했습니다.

## 설치 파일

- `simple-sync-setup.exe`

## 설치 후 확인

- 상단 툴바의 `언어` 콤보박스에서 `한국어`와 `English`를 전환해 화면이 한 번에 갱신되는지 확인합니다.
- 기존 `config.toml` 설정이 유지되는지 확인합니다.
- 필요한 경우 `Sync Now`로 수동 동기화를 실행해 기존 동기화 동작이 유지되는지 확인합니다.
