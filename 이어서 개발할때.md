# 이어서 개발할때

이 문서는 다른 Codex 세션이나 다른 개발자가 `simple sync` 프로젝트를 이어서 개발할 때 필요한 현재 상태와 작업 기준을 정리한 인수인계 문서입니다.

## 프로젝트 위치

```text
C:\workspace\simple-sync
```

## 앱 개요

`simple sync`는 Windows WinForms 기반의 단방향 파일 동기화 앱입니다.

- 소스 디렉터리에서 타겟 디렉터리로만 복사합니다.
- 여러 소스/타겟 쌍을 설정할 수 있습니다.
- 각 sync pair에 이름을 붙여 Activity 로그에서 구분할 수 있습니다.
- 각 sync pair별로 `Copy changes` 또는 `Mirror source` 모드를 선택할 수 있습니다.
- 각 sync pair별 진행률을 표시하고, 선택한 sync pair의 현재 작업을 Activity 영역 상단에 표시합니다.
- Activity 로그는 전체, 선택 pair, 오류 기준으로 필터링할 수 있습니다.
- 지정한 초 단위 간격으로 자동 체크합니다.
- `Sync Now` 버튼으로 즉시 동기화할 수 있습니다.
- 설정은 실행 파일 옆의 `config.toml`로 저장/로드합니다.
- `Add Pair`로 새로 추가한 sync pair는 경로 입력 중 자동 실행되지 않도록 기본 `Off` 상태입니다.
- 내장 스킨 선택값도 `config.toml`에 저장/로드합니다.
- 현재 배포 버전은 `VERSION`에 기록하고, 변경 이력은 `CHANGELOG.md`에 기록합니다.
- Debug 빌드는 Release/설치본과 다른 단일 인스턴스 Mutex를 사용해 설치본과 개발 실행본을 동시에 띄울 수 있습니다.
- Git에는 실제 `config.toml` 대신 `config.example.toml`만 올립니다.
- exe 실행이 제한된 SI 환경을 위해 `scripts/simple-sync.ps1`, `scripts/simple-sync-robocopy.cmd` fallback을 제공합니다.

## 주요 파일

- `Program.cs`: 앱 진입점
- `MainForm.cs`: WinForms UI, 설정 저장, 수동/자동 실행 연결
- `SyncService.cs`: 단방향 파일 복사 로직
- `ConfigService.cs`: `config.toml` 로드/저장
- `Models.cs`: 설정/동기화 결과 모델
- `SimpleSync.csproj`: .NET WinForms 프로젝트 설정, 앱 아이콘 연결
- `AppSkin.cs`: 내장 스킨 색상/스타일 팔레트
- `Assets/simple-sync.ico`: 프로그램 아이콘
- `Assets/simple-sync.svg`: 아이콘 원본
- `Assets/simple-sync-256.png`: 아이콘 미리보기
- `tools/generate_icon.py`: 아이콘 재생성 스크립트
- `scripts/simple-sync.ps1`: exe 제한 환경용 PowerShell fallback
- `scripts/simple-sync-robocopy.cmd`: robocopy 직접 실행용 비상 예제
- `scripts/publish-installer.ps1`: self-contained publish 및 Inno Setup 설치 파일 생성 스크립트
- `installer/simple-sync.iss`: Inno Setup 설치 스크립트
- `README.md`: 사용자용 한국어 설명
- `CHANGELOG.md`: 버전별 변경 이력
- `VERSION`: 현재 배포 버전
- `config.example.toml`: 공유용 설정 예시
- `tests/SimpleSync.Tests`: 외부 테스트 패키지 없이 실행하는 콘솔 검증 프로젝트
- `.gitignore`: Git 제외 규칙

## 현재 구현된 주요 기능

- 다중 sync pair 설정
- sync pair 이름 컬럼과 pair별 Activity 로그 prefix 및 진행경로 출력
- sync pair별 mode 컬럼
- sync pair별 `Progress` 런타임 상태 컬럼
- 선택한 sync pair의 현재 작업 상태 표시
- Activity `All`, `Selected`, `Errors` 필터
- Source/Target 경로 직접 입력
- Source/Target 폴더 선택 버튼
- 신규 sync pair 기본 `Off`
- 초 단위 interval 설정
- Auto 체크박스
- `Sync Now` 즉시 실행
- 변경 파일만 복사
- 임시 파일에 청크 단위로 먼저 복사한 뒤 성공 시 타겟 파일로 교체
- 복사 중 바이트 진행률 보고 및 파일 단위 짧은 재시도
- 기본 `Copy changes` 모드에서는 타겟에만 있는 파일 유지
- `Mirror source` 모드에서는 소스에 없는 타겟 파일/디렉터리 삭제
- 타겟 디렉터리 자동 생성
- 소스와 같거나 소스 내부인 타겟 경로 스킵
- 소스와 타겟이 모두 비어 있는 pair는 저장/로드에서 제외
- 클립보드 텍스트/파일 경로 붙여넣기 방어 처리
- 이미지 등 경로 텍스트가 아닌 클립보드 입력 시 예외 대신 로그 표시
- 앱 창 기본 크기 `1720 x 1120`
- 앱 아이콘 적용
- 내장 스킨 선택 및 마지막 선택값 저장
- 하단 상태 영역에 프로그램 버전 표시
- 단일 인스턴스 Mutex로 중복 실행 방지, 단 Debug 실행은 설치본과 별도 Mutex 사용
- 앱 종료 시 타이머 중지 및 진행 중인 동기화 취소 신호 처리
- `Sync pairs`와 `Activity` 사이를 SplitContainer로 구성해 높이 조절 가능

## 중요한 동작 결정

- 기본 모드는 삭제 동기화를 하지 않는 `copy`입니다.
- `mirror` 모드는 pair별로 명시 선택한 경우에만 타겟에만 있는 파일을 삭제합니다.
- 양방향 동기화는 하지 않습니다.
- `mirror` 모드에서도 소스 탐색 또는 복사 실패가 있으면 삭제 단계는 건너뜁니다.
- 소스에 있는 `.git` 디렉터리는 현재 일반 폴더처럼 복사 대상입니다.
- 앱이 `.git`을 새로 만들지는 않습니다.
- 개인 경로가 들어가는 `config.toml`은 Git에 올리지 않습니다.
- AI/하네스 문서인 `AGENT.md`, `AGENT_kor.md`, `HARNESS.md`, `HARNESS_kor.md`도 Git에 포함합니다.
- 사용자 이미지 스킨은 제외하고, 코드에 정의된 내장 스킨만 제공합니다.

## 실행

개발 중 실행:

```powershell
dotnet run
```

`dotnet run`은 필요한 경우 자동으로 빌드한 뒤 실행합니다.
Debug 실행은 설치된 Release 앱과 별도 인스턴스로 취급되므로, 두 앱을 동시에 띄워 UI와 동작을 비교할 수 있습니다.

## exe 실행 제한 환경 fallback

PowerShell fallback:

```powershell
.\scripts\simple-sync.ps1 -ConfigPath .\config.toml
```

1회 실행:

```powershell
.\scripts\simple-sync.ps1 -ConfigPath .\config.toml -Once
```

비상용 robocopy cmd:

```cmd
scripts\simple-sync-robocopy.cmd "\\wsl.localhost\Ubuntu-24.04\home\u24\projects\spring-lean\docs" "C:\프로젝트 자료\GSNext Phase2\01. 준비"
```

`simple-sync-robocopy.cmd`는 경로를 인자로 받는 최소 예제입니다. 인자를 생략하면 파일 내부의 fallback `SOURCE`, `TARGET` 값을 사용합니다.

fallback 스크립트는 현재 앱 소스와 별도로 유지하며, WinForms 앱 동작을 바꾸지 않습니다.
PowerShell fallback은 `mode = "copy"`를 robocopy `/E`, `mode = "mirror"`를 robocopy `/MIR`로 실행합니다.

## 빌드 검증

```powershell
dotnet build
```

기대 결과:

```text
경고 0개
오류 0개
```

주의: 이미 `dotnet run`으로 실행 중인 앱이 있으면 Debug 출력 파일이 잠겨 빌드가 실패할 수 있습니다. 이 경우 실행 중인 앱을 종료한 뒤 다시 빌드합니다.

## 콘솔 테스트

```powershell
dotnet run --project .\tests\SimpleSync.Tests\SimpleSync.Tests.csproj
```

기대 결과:

```text
PASS reports progress while copying changed files
PASS copy mode preserves target-only files
PASS mirror mode deletes target-only files
```

## 배포용 산출물 생성

현재 버전은 `VERSION` 파일에서 관리합니다. 버전이 바뀌면 `VERSION`, `CHANGELOG.md`, `SimpleSync.csproj` 버전 값을 함께 맞춥니다.

```powershell
dotnet publish -c Release -r win-x64 --self-contained false
```

배포용 실행 파일:

```text
bin\Release\net10.0-windows\win-x64\publish\simple sync.exe
```

이 방식은 framework-dependent publish라 대상 PC에 호환되는 .NET Windows Desktop Runtime이 필요합니다.

## 설치 프로그램 생성

설치 프로그램은 Inno Setup 기반입니다.

staging만 생성:

```powershell
.\scripts\publish-installer.ps1 -SkipInno
```

Inno Setup까지 실행해 setup exe 생성:

```powershell
.\scripts\publish-installer.ps1
```

출력:

```text
dist\simple-sync-setup.exe
```

현재 검증 상태:

- Inno Setup 설치 후 `dist\simple-sync-setup.exe` 생성에 성공했습니다.
- 최신 기능 변경 후에는 `.\scripts\publish-installer.ps1`를 다시 실행해 설치본을 갱신합니다.

## 아이콘 재생성

아이콘을 수정하려면 `tools/generate_icon.py`를 수정한 뒤 실행합니다.

```powershell
& 'C:\Users\ksjeong\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' 'tools\generate_icon.py'
```

생성 파일:

- `Assets/simple-sync.ico`
- `Assets/simple-sync-256.png`

프로젝트 파일에는 이미 다음 설정이 있습니다.

```xml
<ApplicationIcon>Assets\simple-sync.ico</ApplicationIcon>
```

WinForms 창 아이콘은 `MainForm.cs`에서 실행 파일의 연결 아이콘을 읽어 지정합니다.

## Git에 올릴 파일 기준

올릴 파일:

- 앱 소스 `.cs`
- `SimpleSync.csproj`
- `README.md`
- `CHANGELOG.md`
- `VERSION`
- `.gitignore`
- `config.example.toml`
- `Assets/simple-sync.ico`
- `Assets/simple-sync.svg`
- `Assets/simple-sync-256.png`
- `tools/generate_icon.py`
- `scripts/simple-sync.ps1`
- `scripts/simple-sync-robocopy.cmd`
- `scripts/publish-installer.ps1`
- `installer/simple-sync.iss`
- `tests/SimpleSync.Tests`
- `이어서 개발할때.md`
- `AGENT.md`
- `AGENT_kor.md`
- `HARNESS.md`
- `HARNESS_kor.md`

올리지 않을 파일:

- `bin/`
- `obj/`
- `artifacts/`
- `config.toml`
- `*.user`
- `.vs/`
- `Assets/extracted-app-icon.png`

## 수동 테스트 체크리스트

1. `dotnet run`으로 앱 실행
2. interval 값을 변경하고 종료 후 다시 실행해 유지되는지 확인
3. Source/Target을 직접 입력
4. Source/Target 셀에서 `Ctrl+V`로 경로 붙여넣기
5. `Source...`, `Target...` 버튼으로 폴더 선택
6. 2개 이상의 sync pair 추가
7. 한 pair는 enabled, 다른 pair는 disabled로 설정
8. `Sync Now` 클릭 시 enabled pair만 복사되는지 확인
9. sync pair row의 `Progress`와 Activity 상단의 선택 pair 현재 상태가 갱신되는지 확인
10. Activity 필터 `All`, `Selected`, `Errors`가 동작하는지 확인
11. 변경 없는 파일이 다음 실행에서 유지/스킵되는지 확인
12. `Copy changes` 모드에서 타겟에만 있는 파일이 유지되는지 확인
13. `Mirror source` 모드에서 타겟에만 있는 파일이 삭제되는지 확인
14. 타겟을 소스 내부 경로로 지정했을 때 로그에 스킵 메시지가 남는지 확인
15. 존재하지 않는 소스 경로를 넣었을 때 앱이 죽지 않고 로그만 남기는지 확인

## 다음 개선 후보

- `.git`, `.svn`, `.hg`, `bin`, `obj`, `node_modules` 제외 옵션 추가
- 복사 제외 패턴을 `config.toml`에 추가
- 로그 파일 저장 기능 추가
- 실행 중인 동기화를 취소하는 Stop 버튼 추가
- 타겟에 이미 있는 더 최신 파일을 덮어쓸지 여부 옵션 추가

## 최근 이슈와 처리

- 경로 셀에서 백스페이스 입력 시 편집 모드가 풀리는 문제를 수정했습니다.
  - 원인: 저장 중 `_grid.EndEdit()` 호출
  - 처리: 일반 저장은 편집 모드를 유지하고, 앱 종료/Sync Now 실행 시에만 commit

- 텍스트 경로 붙여넣기 중 이미지 클립보드처럼 인식되어 예외 창이 뜨는 문제를 방어했습니다.
  - Source/Target 셀의 `Ctrl+V`를 직접 처리
  - 텍스트 또는 파일 드롭 리스트만 경로로 반영
  - 그 외 클립보드는 로그만 남김

- `dotnet run`에서 창 아이콘이 안 보이는 문제를 수정했습니다.
  - exe 아이콘은 `ApplicationIcon`으로 연결
  - 폼 아이콘은 `Icon.ExtractAssociatedIcon(Application.ExecutablePath)`로 지정

- 비정상 종료/중복 실행으로 프로세스가 남아 파일을 잠그는 문제를 줄였습니다.
  - `Program.cs`에 단일 인스턴스 Mutex 추가
  - 전역 예외 핸들러 추가
  - `MainForm.OnFormClosing`에서 타이머 중지와 동기화 취소 처리

## superpowers 관련

`superpowers`는 OpenAI curated skill 목록에는 없었고, 외부 저장소 방식으로 설치했습니다.

- 저장소: `C:\Users\ksjeong\.codex\superpowers`
- 연결: `C:\Users\ksjeong\.agents\skills\superpowers`

새 스킬 인식은 Codex 재시작 후 가능합니다.
