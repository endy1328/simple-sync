# 변경 이력

`simple sync`의 주요 변경 사항을 기록합니다.

## 1.2.1 - 2026-05-27

- 언어 변경 시 화면 요소가 순차적으로 천천히 바뀌어 보이던 현상을 줄이도록 UI redraw를 일괄 처리하게 개선했습니다.
- 언어 적용 중 Activity 필터 변경 이벤트가 중간 렌더를 일으키지 않도록 조정했습니다.

## 1.2.0 - 2026-05-26

- 상단 툴바에 언어 선택 기능을 추가했습니다. `한국어`와 `English`를 선택할 수 있습니다.
- 선택한 언어를 `config.toml`의 `language` 값으로 저장하고 다음 실행 시 복원합니다.
- 메인 화면 라벨, 동기화 방식 표시명, Activity 상태 문구, 필터 팝업 문구를 다국어화했습니다.
- 내부 동기화 방식 값은 기존처럼 `copy`, `mirror`로 유지해 기존 설정과 호환되도록 했습니다.

## 1.1.1 - 2026-05-26

- `Sync pairs` 목록의 흐름 컬럼을 `Flow`로 명확히 표시하고, 방향 아이콘을 중앙 정렬로 보여주도록 개선했습니다.

## 1.1.0 - 2026-05-26

- 대용량 파일 복사 안정성을 개선했습니다. 임시 파일에 먼저 복사한 뒤 성공 시 타겟 파일로 교체합니다.
- 동기화 쌍별 파일 필터를 추가했습니다. 확장자, 특정 파일, include/exclude 패턴, 하위 디렉터리 포함 여부를 설정할 수 있습니다.
- 필터 프리셋 버튼을 여러 개 누적 선택할 수 있게 하고, 프리셋 값을 `config.toml`에서 수정할 수 있게 했습니다.
- 필터 밖 파일은 `Mirror source`에서도 삭제하지 않도록 안전 정책을 적용했습니다.
- `Sync pairs` 목록에 `Progress` 컬럼을 추가하고, 선택한 쌍의 상태는 Activity 헤더에 표시하도록 정리했습니다.
- 현재 처리 중인 파일이나 경로는 `Progress` 셀 툴팁으로 확인할 수 있게 했습니다.
- Activity 로그에 `All`, `Selected`, `Errors` 필터를 추가했습니다.
- 활성화된 동기화 대상이 없을 때 Activity에 `대상이 없습니다.`를 표시하도록 했습니다.
- progress, copy mode, mirror mode를 검증하는 콘솔 테스트를 추가했습니다.
- Inno Setup 기반 설치 파일 생성 워크플로를 추가했습니다.
- 신규 sync pair는 경로 입력 중 자동 동기화되지 않도록 기본 `Off` 상태로 추가되게 했습니다.
- UI 레이아웃, 스킨, 아이콘, 단일 인스턴스 실행, 창 크기 복원을 개선했습니다.
- 개발 편의를 위해 Debug 실행은 설치된 Release 앱과 별도의 단일 인스턴스 키를 사용하도록 했습니다.
- Debug 실행을 설치본과 구분하기 쉽도록 창 제목과 하단 상태 영역에 `Debug`를 표시합니다.
- 기본 `Copy changes` 모드와 별도로 `Mirror source` 모드를 추가했습니다.
- 앱 화면과 설치 파일에 버전 `1.1.0`을 표시하도록 정리했습니다.

## 1.0.0 - 2026-05-20

- Windows Forms 기반 단방향 폴더 동기화 앱의 초기 안정 버전입니다.
- 여러 source/target sync pair를 지원했습니다.
- 초 단위 자동 동기화와 수동 `Sync Now`를 지원했습니다.
- 설정을 `config.toml`에 저장했습니다.
- source에서 target으로 신규/변경 파일을 복사했습니다.
